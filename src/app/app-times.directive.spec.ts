import { AppTimesDirective } from './app-times.directive';

describe('AppTimesDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTimesDirective();
    expect(directive).toBeTruthy();
  });
});
